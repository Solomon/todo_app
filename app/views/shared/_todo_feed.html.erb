<% unless @feed_items.empty? %>
  <table class="todos" summary="User todos">
  <% @feed_items.each do |feed_item| %>
    <%= render :partial => 'shared/todo_item', :locals => {:feed_item => feed_item} %>
    <% end %>
  </table>
  <%= will_paginate @feed_items %>
<% end %>